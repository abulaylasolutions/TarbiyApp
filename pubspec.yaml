name: tarbiyapp
description: "TarbiyApp - Educazione islamica dei figli"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  intl: ^0.19.0
  provider: ^6.1.0  # o riverpod se usi quello

flutter:
  uses-material-design: true
  assets:
    - assets/avatars/